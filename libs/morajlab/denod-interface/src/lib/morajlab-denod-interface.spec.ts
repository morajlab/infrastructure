import { morajlabDenodInterface } from './morajlab-denod-interface';

describe('morajlabDenodInterface', () => {
  it('should work', () => {
    expect(morajlabDenodInterface()).toEqual('morajlab-denod-interface');
  });
});

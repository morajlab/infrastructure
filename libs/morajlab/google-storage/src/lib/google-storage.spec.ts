import { morajlabGoogleStorage } from './morajlab-google-storage';

describe('morajlabGoogleStorage', () => {
  it('should work', () => {
    expect(morajlabGoogleStorage()).toEqual('morajlab-google-storage');
  });
});
